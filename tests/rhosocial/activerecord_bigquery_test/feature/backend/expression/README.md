# expression/

Expression/SQL formatting: how generic expression-layer constructs render into
BigQuery Standard SQL.

| File | Description |
|------|-------------|
| `test_expressions.py` | Identifier formatting and type mappings (`INTEGER` → `INT64`). |
